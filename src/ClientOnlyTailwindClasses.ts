// because client:only react components do not have their tailwind classes parsed at build time

export const HOME_CARD_CLASS = "bg-white rounded-sm transition-transform cursor-pointer shadow-2xl hover:scale-[102%]";