---

interface Props {
  pdfName: string
}

const { pdfName } = Astro.props;
const pdfUrl = `https://cdn.michaelceryak.com/image/${encodeURIComponent(pdfName)}`;
---

<article class="w-full px-10 ">
  <iframe
    src={pdfUrl}
    class="w-full h-90 lg:h-150"
    title="PDF Preview"
  />
</article>