---
import Layout from "../layouts/Layout.astro";

// import { drizzle } from "drizzle-orm/d1";
// import { homes } from "../../schemas/drizzle";

let officeData = await fetch("https://theofficeapi.dev/api/character/62");
let dataJson = await officeData.json();

// const { DB } = Astro.locals.runtime.env;

// const driz = drizzle(DB);
// console.log(DB.prepare('Select * from Homes').all());

// const homesData = await driz.select().from(homes).all();

// console.log(homesData);
// export const prerender = false;
---

<Layout>
  {JSON.stringify(dataJson)}
</Layout>
